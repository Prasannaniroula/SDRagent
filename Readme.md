# SDR Agent
It is a platform which helps to reduce the workload of Sales Department. This Agent helps in automating the email forwarding for increasing leads and helps in focusing solely for responding to emails or success leads generated by the agent.
<br>
<br>

**Install all the requirements from requirement.txt**

`I have to change here oky.`

**Create a dotenv file by adding these two API**

- Get an API key for any open api like Groq(free) and for paid you can use gpt models. I am using GROQ API but this is a sample of GROQ API not the original API Key
`GROQ_API_KEY=gsk_y7A2bC4dE6fG8hI0jK2lM4nO6pQ8rS0tU2vW4xY6z8`<br>
- We are using SMTP API tool like `SendGrid` or `Brevo` for calculation of metrics like if email is sent, delivered, opened, bounced and other calculations and to reduce redundancy. 
`BREVO_API_KEY=xkeysib-0123456789abcdef0123456789abcdef01234560123456789abcdef-ABCDefghijkLMNOP` 

**Ngrok Installation**<br>
You can directly download the Ngrok from https://ngrok.com/download/ <br>
OR,
Using MAC OS then using Home Brew <br>
`brew install ngrok` <br>
`ngrok config add-authtoken "<YOUR_AUTHTOKEN>"`<br>
Here: For Your_AuthToken you must be logged in so that you will find the authtoken in place of YOUR_AUTHTOKEN <br>
Lastly, Run the NGROK using the command <br>
`ngrok http PORT` you backend running PORT.<br>
For connecting the locals to the internet and to receive metrics form BREVO